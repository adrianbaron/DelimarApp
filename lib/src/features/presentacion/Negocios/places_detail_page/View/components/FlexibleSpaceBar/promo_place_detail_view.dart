import 'package:flutter/material.dart';

class PromoPlaceDetailView extends StatelessWidget {
  PromoPlaceDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 50,
      color: Colors.transparent,
    );
  }
}
